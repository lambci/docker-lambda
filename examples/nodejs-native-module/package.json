{
  "name": "module-native",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "docker run --rm -v \"$PWD\":/var/task lambci/lambda:build-nodejs6.10 npm rebuild",
    "test": "node test.js"
  },
  "dependencies": {
    "bcrypt": "^0.8.6"
  }
}
